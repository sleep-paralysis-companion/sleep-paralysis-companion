# CI Evidence

Workflow: `.github/workflows/phase-1b-foundation.yml`

The final run IDs, URLs, exact runner-reported toolchain versions, test totals, and artifact names
are recorded here only after the pushed branch completes. Until then, Gate 1B is not passed.
